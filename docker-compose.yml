version: '3.8'
services:
  agent:
    build: .
    container_name: nfse-diagnostico-agent
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./data:/app/data
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
